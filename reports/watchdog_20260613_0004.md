# MIA Watchdog Off-Slot Tick — 2026-06-13T00:04Z (run #24)

Off-slot 2h cron tick. Run #23 (22:04Z 12-jun, `5e7b78c`) ya está en
`origin/main`. Próximo on-slot heartbeat: 04:04Z (cruza día UTC dentro
de este intervalo — primer carrier del 13-jun).

**Delta vs run #23 (2h ago):** cero accionable, todo predicho.

- `mcp__github__search_repositories query="user:spenht mia" sort=updated`
  → `spenht/mia-watchdog` `updated_at=2026-06-07T18:05:08Z` (sin cambio
  absoluto, **7º run consecutivo con el mismo timestamp** — está
  petrificado). Silencio acumulado en el repo real:
  **6d 5h 59m ≈ 6d 6h** (era 6d 4h hace 2h, 6d 2h hace 4h, 6d 0h hace
  6h, 5d 22h hace 8h). El reloj corre, nadie commitea: ni Spencer
  manual, ni el on-server orchestrator cron `*/15` del beast.
- `mcp__github__get_file_contents owner=spenht repo=mia-watchdog` →
  `Access denied: repository "spenht/mia-watchdog" is not configured
  for this session. Allowed repositories: spenht/event-ai-ops-v2` —
  scope MCP confirmado clavado al repo equivocado por 7º run seguido.
  Cualquier `push_files` rebotaría igual; no puedo depositar reports
  ahí desde aquí.
- `mia-alan-bridge` (16d desde 28-may) y `MIA_HF` (98d 19h desde
  06-mar) tampoco se mueven; los tres repos MIA visibles siguen sin
  actividad reciente — patrón consistente: nada nuevo en el ecosistema
  visible.
- `git fetch origin main` → ya estaba en `5e7b78c`, sin pushes externos
  en estas 2h. HEAD venía detached del checkout cron; lo reattaché a
  `main` y `reset --hard origin/main` (24 commits behind localmente,
  no había trabajo perdido — sólo el carrier reiniciado del cron cloud).

**Dos blockers humanos persisten desde run #15 (22:04Z 11-jun) — 26h
exactas sin movimiento de Spencer en ninguno (cruza un día calendario
en este tick):**

1. **Cloud trigger misrouted** → code.claude.com → environments →
   trigger MIA watchdog → `repository`: `spenht/event-ai-ops-v2` →
   debe ser `spenht/mia-watchdog`. 7 carriers consecutivos apuntando a
   esto, sin cambio. Es un toggle de UI de ~30 segundos; el costo de
   no hacerlo es que el carrier inteligente Opus 4.7 sigue gastando
   slots cada 2h escribiendo heartbeats al repo equivocado.
2. **Beast on-server orchestrator silente en `spenht/mia-watchdog`** →
   **6d 6h** sin un commit del cron `*/15` del orchestrator confirma
   que esto es un fallo distinto al routing del cloud trigger: posible
   auth roto al remote, URL desactualizada, servicio
   `mia-orchestrator` parado, o cron deshabilitado a nivel `crontab -l`.
   **Aunque flipees el trigger HOY, sin destrabar este push las 9
   preguntas siguen sin datos reales** porque el estado del beast
   (wallet, iters, snipers, buckets) nunca llega al repo que el
   carrier inteligente sí podría leer. Comandos a correr en beast
   cuando puedas (orden de menor a mayor invasivo):
   - `systemctl status mia-orchestrator` — ¿servicio vivo?
   - `journalctl -u mia-orchestrator --since "7 days ago" | tail -100`
     — ¿qué se rompió y cuándo?
   - `crontab -l | grep -i mia` — ¿el cron `*/15` sigue en la tabla?
   - `git -C /opt/mia-watchdog-state remote -v` (o donde viva el
     checkout) — ¿remote URL correcta y reachable?
   - `git -C /opt/mia-watchdog-state log -1 --format=%H%n%cI` — ¿qué
     hash es el último push intentado, y de cuándo es?

**Por qué no escribo las 9 preguntas con datos inventados (Ley #15):**
no hay wallet, iters, snipers ni buckets accesibles en este árbol
(`event-ai-ops-v2` es backend FastAPI de eventos con WhatsApp/Stripe,
sin relación con trading) y la ley #15 prohíbe fabricar métricas.
La ley #21 ("¿esto mueve la aguja a real money esta semana?")
confirma que un report MIA con métricas falsas no mueve aguja — la
confunde. Lo que mueve aguja es la evidencia continua de que el cron
cloud sigue vivo mientras las dos palancas humanas siguen pendientes:
sin esta línea Spencer no distinguiría "cron cloud muerto" de "cron
cloud vivo, esperando fix manual".

— MIA WATCHDOG (Opus 4.7, run #24, off-slot 00:04Z 13-jun, wrong-repo
+ beast-side silencio en mia-watchdog persisten, 0 delta accionable
vs run #23, **6d 6h** acumulados de silencio en el repo real, 26h
exactas sin movimiento de Spencer en los blockers)
