# MIA Watchdog Heartbeat — 2026-06-12T16:04Z (run #20)

On-slot 6h heartbeat (slot 04/10/16/22Z). 2h tras run #19 off-slot, 6h tras
run #18: **0 cambios** en ambos blockers.

- `mcp__github__search_repositories query="user:spenht mia"` →
  `spenht/mia-watchdog` `updated_at=2026-06-07T18:05:08Z`. Ahora **5d 22h**
  de silencio total en ese repo (era 5d 18h en run #18, 5d 20h en run #19).
  El reloj corre, nadie commitea: ni Spencer manual, ni el on-server
  orchestrator cron */15 del beast.
- `mcp__github__search_repositories` lista los 3 repos MIA visibles
  (`mia-watchdog`, `mia-alan-bridge`, `MIA_HF`); ninguno con cambios en
  las últimas 24h. Scope MCP sigue clavado a `spenht/event-ai-ops-v2`
  — `get_file_contents` contra `spenht/mia-watchdog` rebotaría con
  "not configured for this session".
- `git fetch origin main` + `git log` → `origin/main` en `d8fa93f` (run #19).
  Spencer no ha tocado este repo en el intervalo.

**Palanca humana sigue siendo única y precisa, sin progreso desde run #15
(22:04Z 11-jun):** code.claude.com → trigger MIA watchdog → cambiar
`repository` de `spenht/event-ai-ops-v2` → `spenht/mia-watchdog`.

**Segundo blocker, también sin progreso (importa tanto como el switch
cloud):** aunque flipees el trigger HOY, si en beast el `mia_orchestrator.py`
cron */15 no está empujando `status/mia_status_latest.json` a
`spenht/mia-watchdog`, este watchdog seguiría ciego con o sin el switch.
5d 22h sin commits en `mia-watchdog` apunta a algo distinto que el routing:
`journalctl -u mia-orchestrator` + `crontab -l` + `git remote -v` en
`/opt/mia-watchdog-state` (o donde viva el repo en beast) antes de asumir
que basta con cambiar el trigger.

Ley #15: cero métricas fabricadas — no hay wallet, no hay iters, no hay
snipers en este árbol y no me los invento. Ley #21: este heartbeat solo
mueve aguja en la medida en que mantiene evidencia continua de que el
cron cloud sigue vivo; lo que mueve real money es destrabar el repo
correcto y/o el push del orchestrator.

— MIA WATCHDOG (Opus 4.7, run #20, on-slot 16:04Z, wrong-repo + beast-side
silencio en mia-watchdog persisten, 0 delta vs run #19, 5d 22h de silencio
acumulado en el repo real)
