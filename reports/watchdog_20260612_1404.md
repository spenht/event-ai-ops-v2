# MIA Watchdog Off-Slot Tick — 2026-06-12T14:04Z (run #19)

Off-slot 2h cron tick. Run #18 (10:04Z) is on disk + on `origin/main` at
`a02ab70`. The 12:04Z tick went silent (no commit between `a02ab70` and HEAD).
Next on-slot heartbeat is 16:04Z.

**Delta vs run #18 (4h ago):** zero.
- `mcp__github__search_repositories query="user:spenht mia"` →
  `spenht/mia-watchdog` `updated_at=2026-06-07T18:05:08Z` (unchanged for the
  3rd consecutive run; **5d 20h** of total silence on that repo now).
- MCP scope still clamped to `spenht/event-ai-ops-v2` — `get_file_contents`
  against `spenht/mia-watchdog` still rebotaría con "not configured for this
  session".
- `git fetch origin main` → fast-forward `9d154dd..a02ab70`, no new pushes
  from anyone else (Spencer no ha tocado el repo en este intervalo).

**Two distinct blockers, ambos siguen humano-pendiente** (sin progreso
en los últimos 4h):

1. **Cloud trigger misrouted** → code.claude.com → trigger MIA watchdog →
   `repository` field: `spenht/event-ai-ops-v2` → `spenht/mia-watchdog`.
2. **Beast on-server orchestrator silent en `spenht/mia-watchdog`** →
   `journalctl -u mia-orchestrator` o `crontab -l` en beast. Si el
   cron */15 está vivo pero no empuja, el problema no es solo el trigger
   cloud — es algo on-server (auth, remote URL, push deshabilitado).
   Spencer: aunque flipees el trigger HOY, si el orchestrator no empuja
   `status/mia_status_latest.json`, las 9 preguntas seguirán sin datos
   reales y este watchdog seguirá ciego.

Ley #15: cero métricas fabricadas. Ley #21: este heartbeat no mueve aguja
por sí solo — el propósito es **mantener evidencia de que el cron sigue
vivo** mientras la palanca humana está pendiente; sin esta línea Spencer
no distinguiría "cron muerto" de "cron vivo pero esperando fix".

— MIA WATCHDOG (Opus 4.7, run #19, off-slot 14:04Z, wrong-repo + beast-side
silence persisten, 0 delta vs run #18)
