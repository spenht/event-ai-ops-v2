# MIA Watchdog Heartbeat — 2026-06-12T10:04Z (run #18)

6h cycle resumed (slot 04/10/16/22Z). 4h tras delta de run #17, **0 cambios**:

- `origin/main` confirmado en `14d5a0f` (run #17 sí landeó — `mcp__github__list_commits` +
  `git fetch` reconfirman). Los 17 reports + ALERT son visibles en GitHub. Spencer los
  está viendo; no es problema de entrega.
- `mcp__github__search_repositories query="user:spenht mia"` → `spenht/mia-watchdog`
  sigue con `updated_at=2026-06-07T18:05:08Z`. **5 días sin un solo commit ahí.**
  Ni el on-server orchestrator (cron */15 beast) ni acción humana lo han tocado.
- Scope MCP sigue clavado a `event-ai-ops-v2`: `get_file_contents` contra
  `spenht/mia-watchdog` rebotaría con "not configured for this session". Ningún
  `mcp__claude-code-remote__list_repos` / `add_repo` cargable vía ToolSearch
  (reconfirmado este run: `+list_repos claude-code-remote add_repo` → solo trae
  `list_repository_collaborators`).

**Acción humana sigue siendo la misma palanca única, precisa, 1 paso:**
code.claude.com → trigger MIA watchdog → cambiar `repository` de
`spenht/event-ai-ops-v2` → `spenht/mia-watchdog`.

Pregunta secundaria que vale tu atención más que el switch del trigger:
**¿por qué `spenht/mia-watchdog` no ha recibido un commit del on-server
orchestrator en 5 días?** Si beast está corriendo `mia_orchestrator.py` cron */15
escribiendo `status/mia_status_latest.json`, debería empujar. Si no ha empujado,
hay un problema distinto (push deshabilitado, repo equivocado en su lado, o el
orchestrator no está corriendo). Revisar `journalctl -u mia-orchestrator` o
`crontab -l` en beast antes de asumir que solo es el cron cloud.

Ley #15: ni una métrica fabricada. Ley #21: heartbeat este slot mantiene el latido
visible para Spencer, pero no inventa wallet/iters/snipers que no existen aquí.

— MIA WATCHDOG (Opus 4.7, run #18, heartbeat 6h slot 10:04Z, wrong-repo persiste,
beast-side silencio en mia-watchdog también)
