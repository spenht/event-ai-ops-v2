# MIA Watchdog Heartbeat — 2026-06-12T22:04Z (run #23)

On-slot 6h heartbeat (slot 04/10/16/22Z). Run #22 (20:04Z off-slot, `7b715df`)
ya está en `origin/main`. Próximo off-slot tick: 00:04Z.

**Delta vs run #22 (2h ago):** cero accionable, todo predicho.

- `mcp__github__search_repositories query="user:spenht mia" sort=updated` →
  `spenht/mia-watchdog` `updated_at=2026-06-07T18:05:08Z` (sin cambio
  absoluto, **6º run consecutivo con el mismo timestamp**). Silencio
  acumulado en el repo real: **6d 4h** (era 6d 2h hace 2h, 6d 0h hace 4h,
  5d 22h hace 6h). El reloj corre, nadie commitea: ni Spencer manual, ni
  el on-server orchestrator cron `*/15` del beast.
- `mia-alan-bridge` (15d 22h) y `MIA_HF` (98d 18h) tampoco se mueven; los
  tres repos MIA visibles siguen sin actividad reciente.
- Scope MCP sigue clavado a `spenht/event-ai-ops-v2` — cualquier
  `get_file_contents` o `push_files` contra `spenht/mia-watchdog`
  rebotaría con "not configured for this session". Sin palanca humana en
  el cloud trigger esto no destraba desde aquí.
- `git fetch origin main` → ya estaba en `7b715df`, sin pushes externos
  en estas 2h. Reattaché HEAD a `main` (venía detached del checkout
  anterior) y fast-forward los 23 commits que faltaban localmente; sin
  conflictos.

**Dos blockers humanos persisten desde run #15 (22:04Z 11-jun) — 24h
exactas sin movimiento de Spencer en ninguno:**

1. **Cloud trigger misrouted** → code.claude.com → environments →
   trigger MIA watchdog → `repository`: `spenht/event-ai-ops-v2` →
   debe ser `spenht/mia-watchdog`. 6 carriers consecutivos apuntando a
   esto, sin cambio.
2. **Beast on-server orchestrator silente en `spenht/mia-watchdog`** →
   6d 4h sin un commit del cron `*/15` del orchestrator confirma que
   esto es un fallo distinto al routing del cloud trigger: posible auth
   roto al remote, URL desactualizada, servicio `mia-orchestrator`
   parado, o cron deshabilitado a nivel `crontab -l`. **Aunque flipees
   el trigger HOY, sin destrabar este push las 9 preguntas siguen sin
   datos reales.** Comandos a correr en beast cuando puedas:
   - `journalctl -u mia-orchestrator --since "7 days ago" | tail -100`
   - `crontab -l | grep -i mia`
   - `git -C /opt/mia-watchdog-state remote -v` (o donde viva el checkout)
   - `git -C /opt/mia-watchdog-state log -1 --format=%H%n%cI` (último push intentado)

Ley #15: cero métricas fabricadas — no hay wallet, iters, snipers ni
buckets en este árbol y no me los invento. Ley #21: el heartbeat por sí
solo no mueve aguja; lo que mantiene es la evidencia continua de que el
cron cloud sigue vivo mientras las dos palancas humanas siguen pendientes.
Sin esta línea Spencer no distinguiría "cron cloud muerto" de "cron cloud
vivo, esperando fix manual".

— MIA WATCHDOG (Opus 4.7, run #23, on-slot 22:04Z, wrong-repo +
beast-side silencio en mia-watchdog persisten, 0 delta accionable vs
run #22, **6d 4h** acumulados de silencio en el repo real, 24h exactas
sin movimiento de Spencer en los blockers)
