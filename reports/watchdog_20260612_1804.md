# MIA Watchdog Off-Slot Tick — 2026-06-12T18:04Z (run #21)

Off-slot 2h cron tick. Run #20 (16:04Z, `acb7b27`) is on `origin/main`. Next
on-slot heartbeat is 22:04Z.

**Delta vs run #20 (2h ago):** zero.
- `mcp__github__search_repositories query="user:spenht mia" sort=updated` →
  `spenht/mia-watchdog` `updated_at=2026-06-07T18:05:08Z` (unchanged for the
  5th consecutive run; **5d 23h 59m** of total silence on that repo now —
  cruzando el umbral de 6 días al cierre de este tick).
- Scope MCP sigue clavado a `spenht/event-ai-ops-v2` — `get_file_contents`
  contra `spenht/mia-watchdog` seguiría rebotando con "not configured for
  this session".
- `git fetch origin main` → fast-forward `9d154dd..acb7b27`, sin pushes
  externos (HEAD venía detached, lo reattacho a `main` antes de commitear).

**Dos blockers humanos, ambos sin progreso desde run #15 (22:04Z 11-jun):**

1. **Cloud trigger misrouted** → code.claude.com → trigger MIA watchdog →
   `repository`: `spenht/event-ai-ops-v2` → `spenht/mia-watchdog`.
2. **Beast on-server orchestrator silent en `spenht/mia-watchdog`** →
   `journalctl -u mia-orchestrator` o `crontab -l` en beast. Casi 6 días
   sin un commit sugiere que el cron */15 no está empujando (auth, remote
   URL, push deshabilitado, o el servicio detenido). Aunque flipees el
   trigger HOY, sin este push las 9 preguntas siguen sin datos reales.

Ley #15: cero métricas fabricadas — no hay wallet, iters ni snipers en este
árbol y no me los invento. Ley #21: el heartbeat por sí solo no mueve aguja;
mantiene evidencia de que el cron cloud sigue vivo mientras la palanca humana
está pendiente. Sin esta línea Spencer no distinguiría "cron muerto" de
"cron vivo, esperando fix".

— MIA WATCHDOG (Opus 4.7, run #21, off-slot 18:04Z, wrong-repo + beast-side
silencio en mia-watchdog persisten, 0 delta vs run #20, ~6 días acumulados
de silencio en el repo real)
